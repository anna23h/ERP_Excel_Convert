# 2026-06-13

## 改了什么
- 新增多机同步工作流脚手架（不动任何现有脚本）：`CLAUDE.md`（工作前读日志 / 工作后写日志 / 多机协作约定）、`docs/journal/TEMPLATE.md`、`config.example.py`；`.gitignore` 补 `test-data/` `results/` `config.py`。
- `cp config.example.py config.py`（本机路径待填，已 gitignore）。
- 补写 `docs/journal/` 历史日志 2026-06-10 ~ 06-12（依据 Claude Code Changelog 与 git 历史整理）。

## 决策
- 数据源路径走 `config.py`（gitignore），而非硬编码进脚本或提交。
- 测试数据/结果走 **Syncthing** 在两台 Mac 间同步，不走 git（git 只承载逻辑与文档）。
- 机器分工：iMac（开发，历史数据）/ MacBook Pro（开发 + 实时数据测试）/ Windows（仅测试目标，从不在此改逻辑）。

## 遗留 / 下一步
- `config.py` 填本机实际路径。
- 脚手架 commit `ef845c7` 未 push，待用户检查后自行推。
- 上一会话遗留：GUI 输入区按 ERP 销售/库存模块重排（见 06-12）。

## 测试
- 无（脚手架，无逻辑改动）。
